pipeline {
  agent any
  stages {
    stage('pre-build') {
      steps {
        echo 'this is pre build stage'
      }
    }

    stage('build') {
      steps {
        echo 'build'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}